#ifndef BACKLACE_LTCGI_CGINC
#define BACKLACE_LTCGI_CGINC

// the user needs this package for this to work, it isn't bundled with Backlace
// just a wrapper for organisation ^~^
#include "Packages/at.pimaker.ltcgi/Shaders/LTCGI.cginc"

#endif // BACKLACE_LTCGI_CGINC